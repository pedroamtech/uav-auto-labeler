# uav-auto-labeler
Auto-labeling tool for aerial/UAV imagery using a YOLOv8 model fine-tuned on VisDrone — generates YOLO-format labels (pedestrian + people merged into a single "person" class) via batched GPU inference, with automatic model download from Hugging Face Hub.
